/**
 * Public surface of the auth module — the only things other code (the
 * router) is allowed to import from this module. Everything else
 * (api/, hooks/, validation/, components/, types/) is an internal
 * implementation detail.
 */
export { LoginPage } from "@/modules/auth/pages/LoginPage";
