"""Cross-module unit tests.

Empty until more than one module exists to test interactions between.
Per-module unit tests live inside each app's own tests/unit/ directory
instead (see apps/healthcheck/tests/unit/ for the pattern).
"""
